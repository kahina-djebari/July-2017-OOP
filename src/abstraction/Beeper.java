package abstraction;

public interface Beeper {
//in an interface we can only declare final static variable (final static int x = 5)
//the interface is implicitly abstract and so are the methods in the interface,
//we do not need to use the abstract key word
}
